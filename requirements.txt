# Core training/eval 
numpy>=2.1.2
pandas>=2.2.3
scikit-learn>=1.6.1
matplotlib>=3.10.0
scipy>=1.15.0
joblib>=1.5.0
pyarrow>=20.0.0  

# Notebooks + exploratory scripts
jupyter>=1.0.0
ipykernel>=6.29.5
seaborn>=0.13.0
# plotter.py only (interactive hover). Caution: mplcursors>=0.7 forces
# matplotlib!=3.10.3, i.e. past the paper env (3.10.3) — install it in a
# separate env if figure reproducibility matters.
mplcursors>=0.5.1
